# macOS
.DS_Store

# Xcode / Swift build artifacts
build/
DerivedData/
*.xcodeproj/
.dd-cloud/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
.swiftpm/
.build/

# Xcode per-user state (track shared project, ignore user-specific)
xcuserdata/
*.xcuserdatad

# 图标中间产物
icon/*.iconset/

# Keep raw demo captures locally; publish only reviewed final media.
promotion/video/*
!promotion/video/clip-demo.mp4
!promotion/video/clip-editor.png
!promotion/video/clip-competitors.mp4
