# TL Clipbook

自用的 macOS 剪贴板历史。菜单栏常驻，`⌘⇧V` 唤出，回车粘贴。全 Swift，无网络，数据只在本机。

- 记录文本 / 链接 / 图片 / 文件，来源 app 与时间一起记；同内容再复制只顶上不重复
- 子串搜索（中文可直接搜），置顶，`⌘⌫` 删除
- 回车粘贴到你原来的 app（授权「辅助功能」后自动补 `⌘V`；没授权就只放进剪贴板）
- 跳过密码管理器标记的内容
- `clipbook://show?q=…` 给自动化用

构建：`./build.sh`（装到 /Applications/TL Clipbook.app）。
